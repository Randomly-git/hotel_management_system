server:
  port: 8082
  address: 0.0.0.0  # 可选，允许外部访问

spring:
  datasource:
    url: jdbc:mysql://47.100.240.111:3306/hotel_management_system?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8&allowPublicKeyRetrieval=true
    username: root
    password: Db123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true


# 智谱AI配置
zhipu:
  ai:
    api-key: 73a648182d3245878e818b010c6a0cc2.Defsf532071zJDfo
    api-url: https://open.bigmodel.cn/api/paas/v4/chat/completions
    model: glm-4-flash
    timeout: 30000
    retry-count: 3

# 开发环境日志级别
logging:
  level:
    com.petcare.backend: DEBUG
    org.hibernate.SQL: DEBUG