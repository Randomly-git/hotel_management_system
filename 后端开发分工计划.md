# 精品单体酒店智能管理系统 - 后端开发分工计划

## 项目概述

本文档详细说明了后端开发的分工安排、开发规范和交付要求。开发周期为10天，目标是完成符合需求规约的完整后端系统。

## 📌 项目约束和要求

### 必须遵守的技术规范

1. **技术栈要求**
   - 后端框架：Java Spring Boot 4.0.0
   - 数据库：MySQL（必须使用）
   - 缓存：Redis（必须使用）
   - NLP服务：必须集成第三方SDK（百度AI/讯飞等）

2. **安全要求**
   - 所有核心表必须包含`hotel_id`字段实现租户隔离
   - 客户身份证号、手机号必须AES加密存储
   - API必须使用HTTPS（开发环境可暂时使用HTTP）
   - 符合《个人信息保护法》要求

3. **性能要求**
   - NLP解析响应时间 ≤ 5秒
   - 定价计算时间 ≤ 10秒
   - 普通接口响应时间 ≤ 3秒

4. **架构要求**
   - 模块化设计，每个功能模块独立
   - 统一的响应格式
   - 完善的异常处理
   - 完整的API文档

## 👥 团队分工

### 成员A：口碑量化系统负责人

#### 负责模块
- F1：口碑量化系统（Reputation Quantification System）

#### 主要任务清单

##### 第一阶段（Day 1-3）：基础准备
1. **数据库设计完善**
   - 在`CustomerFeedback`表添加`needsReview`字段（Boolean）
   - 在`CustomerFeedback`表添加`reviewStatus`字段（PENDING/APPROVED/REJECTED）
   - 在`DepartmentPerformance`表添加`hotel_id`字段

2. **实体类更新**
   ```java
   // CustomerFeedback.java 新增字段
   @Column(name = "needs_review")
   private Boolean needsReview;

   @Column(name = "review_status")
   private String reviewStatus; // PENDING/APPROVED/REJECTED
   ```

3. **Controller层实现**
   - 完善`CustomerFeedbackController`
   - 新增反馈审核接口

##### 第二阶段（Day 4-6）：核心功能实现
1. **实现F1.1：每日绩效自动计算（UC-F1-01）**
   - 创建`PerformanceCalculationScheduler`类
   - 实现每日凌晨2点自动触发的定时任务
   - 实现完整的计算流程：
     - 查询未处理反馈（isProcessed=false）
     - 计算ScoreIndex = Avg(NormalizedSentiment) * 100
     - UPSERT到DepartmentPerformance表
     - 批量标记反馈为已处理

2. **实现F1.2：预警等级判定**
   - 实现预警规则：
     - RED: ScoreIndex < 70
     - YELLOW: 70 ≤ ScoreIndex < 80
     - GREEN: ScoreIndex ≥ 80
   - 创建预警通知服务接口

3. **恶意评论风险防控**
   - 实现审核机制：
     - 情感分 < -0.8的反馈自动标记为needsReview=true
     - 同一IP短时间（如1小时内）超过5条反馈触发审核
   - 实现敏感词过滤（可使用简单关键词列表）

##### 第三阶段（Day 7-10）：优化与测试
1. **批量处理优化**
   - 实现反馈批量处理接口
   - 优化定时任务性能

2. **单元测试编写**
   - PerformanceCalculationService测试
   - 边界条件测试
   - 异常情况测试

#### 需要实现的接口清单

```java
// 1. 反馈审核接口
POST /api/v1/feedback/review
{
    "feedbackId": 1,
    "reviewStatus": "APPROVED", // APPROVED/REJECTED
    "reviewComment": "审核意见"
}

// 2. 获取待审核反馈
GET /api/v1/feedback/pending-review

// 3. 手动触发绩效计算
POST /api/v1/performance/calculate
{
    "targetDate": "2024-12-16"
}

// 4. 获取当前预警信息
GET /api/v1/performance/alerts
```

---

### 成员B：个性化服务系统负责人

#### 负责模块
- F2：个性化服务系统（Personalized Service System）
- 基础设施建设

#### 主要任务清单

##### 第一阶段（Day 1-3）：基础设施
1. **SpringDoc OpenAPI集成**
   - 在pom.xml添加依赖
   - 配置OpenAPI信息
   - 为所有Controller添加@Tag注解

2. **统一响应格式实现**
   ```java
   @Data
   public class ApiResponse<T> {
       private Integer code;
       private String message;
       private T data;
       private Long timestamp;
   }
   ```

3. **安全基础实现**
   - 实现租户数据拦截器
   - 实现AES加密工具类
   - 配置所有接口的hotel_id验证

##### 第二阶段（Day 4-6）：核心功能实现
1. **实现F2.1：自然语言需求解析**
   - 集成百度AI SDK（或讯飞开放平台）
   - 实现意图识别：
     - 房间相关：房型、设施、布置
     - 服务相关：清洁、餐饮、接送
     - 物品相关：用品、设备、特殊需求
   - 实现实体提取：
     - 时间实体："下午2点前" → 14:00
     - 数量实体："两瓶水" → 2, 水

2. **实现F2.2：任务订单自动分派**
   - 完善TaskOrder实体
   - 实现任务-部门匹配规则
   - 实现任务状态流转：
     - PENDING → IN_PROGRESS → COMPLETED
     - 支持任务取消（CANCELED）

3. **实现F2.4：宾客偏好数据沉淀**
   - 每次任务完成后提取偏好
   - 更新GuestProfile的标签
   - 实现偏好权重计算

##### 第三阶段（Day 7-10）：优化与缓存
1. **Redis缓存实现**
   - 缓存GuestProfile数据
   - 缓存热门任务模板
   - 实现缓存更新策略

2. **NLP服务降级策略**
   - 当NLP服务不可用时的关键词匹配
   - 置信度低于阈值时标记为"需人工确认"
   - 异常输入清洗

#### 需要实现的接口清单

```java
// 1. 客户画像查询
GET /api/v1/guest-profile/{memberId}

// 2. 更新客户画像
PUT /api/v1/guest-profile/{memberId}
{
    "preferences": ["安静", "高楼层", "硬床垫"],
    "tags": ["商务客", "常住客"],
    "lastStayRoom": "801"
}

// 3. 批量处理任务
POST /api/v1/personalization/tasks/batch
{
    "taskIds": [1, 2, 3],
    "action": "ASSIGN" // ASSIGN/COMPLETE/CANCEL
}

// 4. 获取任务统计
GET /api/v1/personalization/tasks/statistics
```

---

### 成员C：动态定价系统负责人

#### 负责模块
- F3：动态定价系统（Dynamic Pricing System）
- 集成测试

#### 主要任务清单

##### 第一阶段（Day 1-3）：数据准备
1. **完善房型管理**
   - 实现RoomType的完整CRUD
   - 添加房型特征标签（景观、面积、设施）
   - 实现房型库存管理

2. **基础数据采集接口**
   - 竞品价格抓取模拟（使用随机数据）
   - 本地事件数据接口
   - 历史入住率查询接口

##### 第二阶段（Day 4-6）：核心算法实现
1. **实现F3.2：智能调价建议生成**
   - 多因子定价模型：
     ```
     调整因子 = 季节系数 × 竞争因子 × 需求因子 × 活动因子
     建议价格 = 基准价 × 调整因子
     ```
   - 价格合理性检查：
     - 调整幅度 > 50% 触发二级审批
     - 设置价格上下限
   - 实现未来7天价格计算

2. **实现F3.3：人工审批与价格锁定**
   - 审批流程实现
   - 价格锁定机制
   - 审批记录日志

3. **实现F3.4：全渠道价格同步**
   - 模拟渠道同步接口
   - 同步日志记录
   - 失败重试机制

##### 第三阶段（Day 7-10）：测试与性能
1. **集成测试编写**
   - 编写完整的测试用例
   - 使用TestContainers进行集成测试
   - 性能测试（确保≤10秒）

2. **监控与日志**
   - 关键操作日志记录
   - 性能监控埋点
   - 异常统计

#### 需要实现的接口清单

```java
// 1. 房型管理
GET /api/v1/room-types
POST /api/v1/room-types
PUT /api/v1/room-types/{id}

// 2. 价格审核
GET /api/v1/pricing/pending-approval
POST /api/v1/pricing/approve
{
    "recordId": 1,
    "approvedPrice": 888,
    "approvalComment": "同意调整"
}

// 3. 渠道同步状态
GET /api/v1/pricing/sync-status
{
    "date": "2024-12-16",
    "channels": ["官网", "携程", "美团"]
}

// 4. 定价策略配置
PUT /api/v1/pricing/strategy
{
    "seasonFactor": 1.2,
    "competitionWeight": 0.3,
    "maxAdjustmentRate": 0.5
}
```

## 📋 开发规范

### 代码规范

1. **命名规范**
   - 类名：PascalCase（如：CustomerFeedbackService）
   - 方法名：camelCase（如：calculatePerformance）
   - 常量：UPPER_SNAKE_CASE（如：MAX_RETRY_COUNT）

2. **注释规范**
   - 所有public方法必须有JavaDoc
   - 复杂逻辑必须添加行内注释
   - API接口必须有Operation注解

3. **异常处理**
   - 使用自定义异常类
   - 统一异常处理器
   - 明确的错误码和错误信息

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    @ExceptionHandler(BusinessException.class)
    public ResponseEntity<ApiResponse<Void>> handleBusinessException(BusinessException e) {
        return ResponseEntity.badRequest()
            .body(ApiResponse.error(e.getCode(), e.getMessage()));
    }
}
```

### Git规范

1. **分支管理**
   - main：主分支，只接受merge request
   - develop：开发分支
   - feature/F1、feature/F2、feature/F3：功能分支

2. **提交信息格式**
   ```
   type(scope): description

   [optional body]

   [optional footer]
   ```
   - type: feat/fix/docs/style/refactor/test
   - scope: 模块名
   - description: 简短描述

3. **代码审查**
   - 每个PR必须至少一人审查
   - 审查重点：功能完整性、性能、安全性、代码规范

### API设计规范

1. **RESTful规范**
   - GET：查询
   - POST：创建
   - PUT：更新
   - DELETE：删除

2. **统一响应格式**
   ```json
   {
     "code": 200,
     "message": "success",
     "data": {},
     "timestamp": 1641234567890
   }
   ```

3. **错误码定义**
   - 200：成功
   - 400：请求参数错误
   - 401：未授权
   - 403：无权限
   - 404：资源不存在
   - 500：服务器内部错误

## 📅 时间计划

### Day 1-3：基础搭建
- [ ] SpringDoc集成（成员B）
- [ ] 数据库字段完善（全员）
- [ ] 统一响应格式（成员B）
- [ ] 基础CRUD接口（成员C）

### Day 4-6：核心功能
- [ ] F1.1绩效计算（成员A）
- [ ] F2.1 NLP集成（成员B）
- [ ] F3.2定价算法（成员C）
- [ ] 安全功能实现（成员B）

### Day 7-8：高级功能
- [ ] 预警机制（成员A）
- [ ] 任务分派（成员B）
- [ ] 价格审批（成员C）

### Day 9-10：测试与优化
- [ ] 单元测试编写（全员）
- [ ] 集成测试（成员C主导）
- [ ] 性能优化（全员）
- [ ] 文档完善（全员）

## ✅ 验收标准

### 功能验收
- [ ] 所有需求规约中的功能点已实现
- [ ] 所有用例规约中的流程已完成
- [ ] 异常流程已处理

### 质量验收
- [ ] 单元测试覆盖率 ≥ 70%
- [ ] 接口响应时间满足性能要求
- [ ] 代码规范检查通过
- [ ] 安全扫描通过

### 文档验收
- [ ] Swagger UI可访问且文档完整
- [ ] API文档已更新
- [ ] 部署文档已编写
- [ ] 测试报告已完成

## 🚀 交付清单

1. **代码交付**
   - 功能完整的后端应用
   - 完整的测试用例
   - 数据库初始化脚本

2. **文档交付**
   - API接口文档（Swagger）
   - 部署说明文档
   - 系统配置文档

3. **演示材料**
   - Postman测试集合
   - 关键接口演示脚本
   - 性能测试报告

## ⚠️ 注意事项

1. **数据安全**
   - 生产数据不可使用真实客户信息
   - 密码、密钥等敏感信息不可提交到代码仓库
   - 数据库连接信息使用环境变量

2. **性能优化**
   - 数据库查询必须有索引
   - 避免N+1查询问题
   - 合理使用缓存

3. **异常处理**
   - 所有外部调用必须有超时设置
   - 关键操作必须有重试机制
   - 详细的错误日志记录

## 📞 联系方式

- 技术问题：群内讨论
- 进度同步：每日晨会
- 紧急问题：电话联系

---

**文档版本**：v1.0
**更新日期**：2024-12-16
**制定人**：项目组