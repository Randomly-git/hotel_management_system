# 项目公网访问部署方案

## 问题说明

本地 localhost (127.0.0.1) 只能在本机访问，其他人无法访问。需要将服务部署到公网，让老师和同学可以通过浏览器访问。

## 解决方案对比

### 方案一：免费云服务器（推荐）

#### 1. 阿里云/腾讯云学生机
- **费用**：学生认证后每月约¥10-24
- **配置**：1核2G，1M带宽
- **优点**：稳定可靠，有固定IP
- **申请地址**：
  - 阿里云：https://developer.aliyun.com/plan/
  - 腾讯云：https://cloud.tencent.com/act/campus

#### 2. 华为云学生套餐
- **费用**：免费试用1年
- **配置**：1核1G
- **优点**：完全免费
- **申请地址**：https://www.huaweicloud.com/campaign/

### 方案二：内网穿透工具（临时测试用）

#### 1. cpolar（免费）
```bash
# 下载安装
# Windows: https://www.cpolar.com/download

# 启动内网穿透
cpolar http 8080

# 会生成类似地址：
# https://random-name.cpolar.io -> http://localhost:8080
```

#### 2. frp（需要服务器）
如果有云服务器，可以使用frp进行内网穿透

### 方案三：容器化部署

#### 1. Docker + 云服务器
将应用打包成Docker镜像，部署到云服务器

#### 2. Railway（免费额度）
- GitHub账号登录
- 直接从GitHub部署
- 每月有免费额度
- 地址：https://railway.app/

## 详细实施步骤

### 推荐方案：阿里云学生机部署

#### 第一步：申请服务器

1. 访问 https://developer.aliyun.com/plan/
2. 完成学生认证（需要学信网认证）
3. 购买ECS云服务器（选择Ubuntu 20.04）
4. 记录公网IP地址

#### 第二步：服务器环境配置

```bash
# 1. 连接服务器
ssh root@你的公网IP

# 2. 安装Java 21
wget https://download.oracle.com/java/21/latest/jdk-21_linux-x64_bin.tar.gz
tar -xzf jdk-21_linux-x64_bin.tar.gz
mv jdk-21.0.1 /usr/local/java
echo 'export JAVA_HOME=/usr/local/java' >> ~/.bashrc
echo 'export PATH=$PATH:$JAVA_HOME/bin' >> ~/.bashrc
source ~/.bashrc

# 3. 安装MySQL 8.0
apt update
apt install mysql-server -y

# 4. 安装Redis
apt install redis-server -y

# 5. 安装Nginx（用于前端部署）
apt install nginx -y

# 6. 安装Maven
apt install maven -y
```

#### 第三步：数据库配置

```bash
# 登录MySQL
mysql -u root -p

# 创建数据库和用户
CREATE DATABASE hotel_system;
CREATE USER 'hoteluser'@'%' IDENTIFIED BY 'yourpassword';
GRANT ALL PRIVILEGES ON hotel_system.* TO 'hoteluser'@'%';
FLUSH PRIVILEGES;

# 修改MySQL配置，允许远程连接
nano /etc/mysql/mysql.conf.d/mysqld.cnf
# 修改 bind-address = 0.0.0.0

# 重启MySQL
systemctl restart mysql
```

#### 第四步：应用部署

```bash
# 1. 创建项目目录
mkdir /opt/hotel-system
cd /opt/hotel-system

# 2. 上传代码包
# 使用scp命令上传本地打包的jar文件
scp backend/hotel/target/hotel-0.0.1-SNAPSHOT.jar root@你的公网IP:/opt/hotel-system/

# 3. 修改配置文件
nano application.properties

# 修改为：
server.port=8080
spring.datasource.url=jdbc:mysql://localhost:3306/hotel_system
spring.datasource.username=hoteluser
spring.datasource.password=yourpassword

# 4. 启动应用
nohup java -jar hotel-0.0.1-SNAPSHOT.jar > app.log 2>&1 &

# 5. 查看日志
tail -f app.log
```

#### 第五步：安全配置

```bash
# 1. 开放防火墙端口
ufw allow 80
ufw allow 443
ufw allow 8080
ufw enable

# 2. 在阿里云控制台配置安全组
# 添加入方向规则：
# - 端口范围：80/80, 443/443, 8080/8080
# - 授权对象：0.0.0.0/0
```

### 备选方案：使用cpolar（临时测试）

如果来不及申请云服务器，可以使用内网穿透临时解决：

#### 1. 下载cpolar
```bash
# Windows版下载地址
https://www.cpolar.com/downloads
```

#### 2. 安装配置
```bash
# 安装后启动
cpolar http 8080

# 生成访问地址，类似：
# http://abcd1234.cpolar.io
```

#### 3. 启动Spring Boot应用
```bash
# 确保应用在8080端口运行
mvn spring-boot:run
```

#### 4. 访问测试
通过 cpolar 提供的地址访问：
- 后端API：http://abcd1234.cpolar.io/swagger-ui.html
- 接口访问：http://abcd1234.cpolar.io/api/v1/...

## Nginx配置（用于生产环境）

创建 `/etc/nginx/sites-available/hotel-system` 文件：

```nginx
server {
    listen 80;
    server_name 你的公网IP;

    # 后端API代理
    location /api/ {
        proxy_pass http://localhost:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # 前端静态文件
    location / {
        root /opt/hotel-system/frontend/dist;
        try_files $uri $uri/ /index.html;
    }
}
```

启用配置：
```bash
ln -s /etc/nginx/sites-available/hotel-system /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx
```

## 域名配置（可选）

如果需要域名访问：

1. 购买域名（阿里云/腾讯云）
2. 解析到云服务器IP
3. 配置Nginx的server_name为域名
4. 申请免费SSL证书（Let's Encrypt）

## 监控和维护

### 1. 查看应用状态
```bash
# 查看Java进程
ps aux | grep java

# 查看日志
tail -f /opt/hotel-system/app.log

# 重启应用
pkill -f "hotel-0.0.1-SNAPSHOT.jar"
cd /opt/hotel-system && nohup java -jar hotel-0.0.1-SNAPSHOT.jar > app.log 2>&1 &
```

### 2. 自动启动脚本
创建 `/etc/systemd/system/hotel-system.service`：

```ini
[Unit]
Description=Hotel Management System
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/hotel-system
ExecStart=/usr/local/java/bin/java -jar hotel-0.0.1-SNAPSHOT.jar
Restart=always

[Install]
WantedBy=multi-user.target
```

启用自启：
```bash
systemctl daemon-reload
systemctl enable hotel-system
systemctl start hotel-system
```

## 成本估算

| 方案 | 月费用 | 优点 | 缺点 |
|------|--------|------|------|
| 阿里云学生机 | ¥10-24 | 稳定、固定IP | 需要认证 |
| 华为云免费版 | ¥0 | 完全免费 | 配置较低 |
| cpolar | ¥0 | 免费快速 | 不稳定、限速 |
| Railway | ¥0-20 | 简单部署 | 有流量限制 |

## 推荐流程

1. **立即方案**（1小时内完成）：
   - 使用cpolar内网穿透
   - 快速演示使用

2. **短期方案**（1-2天）：
   - 申请华为云免费服务器
   - 部署基础版本

3. **长期方案**（1周内）：
   - 申请阿里云学生机
   - 完整部署和优化

## 注意事项

1. **安全考虑**：
   - 使用强密码
   - 定期备份数据库
   - 及时更新系统补丁

2. **性能优化**：
   - 配置数据库连接池
   - 使用Redis缓存
   - 监控系统资源

3. **备份策略**：
   ```bash
   # 数据库备份脚本
   #!/bin/bash
   mysqldump -u hoteluser -p hotel_system > /backup/hotel_$(date +%Y%m%d).sql
   ```

## 快速访问地址格式

部署完成后，访问地址为：
- 后端API：http://你的公网IP:8080
- Swagger文档：http://你的公网IP:8080/swagger-ui.html
- 前端页面：http://你的公网IP（配置Nginx后）

老师可以通过这些地址访问你的项目进行评分。