# 🏨 精品酒店智能管理系统 - 后端接口规范 (2025版)

本项目已实现基于 **AI 动态调价算法** 与 **多日阶梯计费** 的核心引擎。后端自动处理竞品分析、占用率加成及季节性波动，确保酒店收益最大化。

---

## 🚀 核心逻辑说明 (前端必读)

1. **阶梯计费逻辑**：预订总额不是“单价 × 天数”。后端会逐日计算每一天的动态房价并求和。
2. **保底机制**：如果查询日期（如远期预订）未生成动态调价记录，系统将自动回退（Fallback）至房型的 **基础底价**，确保业务不中断。
3. **价格生命周期**：
    - `adjust` (计算) -> 正常价格 -> `APPROVED` (前端可见)
    - `adjust` (计算) -> 异常波动 -> `PENDING` (需店长审批后方可预订)

---

## 🛠️ 定价模块接口 (`PricingController`)

| 动作 | 接口路径 | 方式 | 参数说明 | 业务场景 |
| :--- | :--- | :--- | :--- | :--- |
| **一键调价** | `/api/v1/pricing/adjust` | `POST` | 无 | **核心接口**。触发未来 30 天的 AI 调价计算。 |
| **查询房价** | `/api/v1/pricing/current` | `GET` | `date` (YYYY-MM-DD) | 获取指定日期所有房型的已生效动态价格。 |
| **暴力改价** | `/api/v1/pricing/manual-update` | `PUT` | `recordId`, `newPrice`, `reason` | 店长无视算法，手动强制锁定某日价格。 |
| **修改底价** | `/api/v1/pricing/base-price` | `PUT` | `typeId`, `newBasePrice` | 修改房型挂牌价。修改后需调 `adjust` 刷新。 |
| **待审清单** | `/api/v1/pricing/review` | `GET` | 无 | 获取波动超过 50% 需要人工介入的价格记录。 |
| **审批通过** | `/api/v1/pricing/approve` | `POST` | `recordId` | 将待审核记录状态转为已通过。 |
| **采集行情** | `/api/v1/pricing/competitors/collect` | `POST` | 无 | 仅刷新外部竞品价格，不影响内部定价。 |
| **查看行情** | `/api/v1/pricing/competitors` | `GET` | 无 | 获取所有采集到的竞争对手价格明细。 |

---

## 🛒 预订模块接口 (`BookingController`)

| 动作 | 接口路径 | 方式 | 参数说明 | 业务场景 |
| :--- | :--- | :--- | :--- | :--- |
| **房态日历** | `/api/v1/booking/room-calendar/{roomId}` | `GET` | `roomId` (路径变量) | 返回该房间已被占用的日期列表。用于禁选日期。 |
| **提交预订** | `/api/v1/booking/reserve` | `POST` | `roomId`, `startTime`, `days` | **核心接口**。提交后返回包含总价和计费明细的订单。 |

---

## 📊 状态码定义
- `200 OK`: 请求成功。
- `204 No Content`: 该日期没有定价记录（通常会回退到基础价）。
- `400 Bad Request`: 参数格式错误（如日期格式不对）。
- `500 Internal Server Error`: 业务逻辑错误（如预订冲突、底价缺失）。

---

## 📝 开发备注
- **日期格式**：所有日期接口均遵循 `ISO-8601` 标准，即 `YYYY-MM-DD`。
- **金额单位**：所有金额字段均返回 `BigDecimal` 类型，建议前端保留两位小数。
- **订单备注**：订单创建成功后，`remark` 字段中包含了每日价格拆分明细，可用于前端对账展示。